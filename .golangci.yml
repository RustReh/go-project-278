version: "2"

linters:
  default: standard
  settings:
    govet:
      disable:
        - fieldalignment

formatters:
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
