version: "2"
sql:
  - engine: "postgresql"
    schema: "migrations"
    queries: "queries"
    gen:
      go:
        package: "sqlc"
        out: "../internal/db/sqlc"
        emit_interface: true
        emit_prepared_queries: true
